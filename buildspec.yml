version: 0.2
env:
  secrets-manager:
    TOKEN: firstSecretForSonar:tokenforSonar
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=kishiaws-devsecops-org01 -Dsonar.organization=kishiaws-devsecops-org01 -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=$TOKEN 
